# realhands Bridge — full dependency pin set.
# Includes deps for the credential vault (vault.py, built in parallel) so the
# whole agent-bridge subpackage installs from one file.
#
# Pinned exactly; bump deliberately.

# ---- bridge core ----
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
httpx==0.28.1

# ---- vault (sibling file) ----
cryptography==44.0.0
keyring==25.5.0

# ---- test-only ----
pytest==8.3.4
pytest-asyncio==0.25.0
websockets==14.1
